[pytest]
; Default `pytest` invocation (no args) only runs the harness's own test
; suite. Task fixtures under tasks/*/visible_tests and tasks/*/hidden_tests
; are intentionally excluded here -- they are executed on demand, in their
; own subprocess, by experiment.runner.PytestRunner (and by the CLI), never
; as part of the ambient backend test suite.
testpaths = tests
